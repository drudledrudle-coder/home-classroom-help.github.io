[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "22"

# Room API is a single function; /api/room is the only server surface the app has.
[[redirects]]
  from = "/api/room"
  to = "/.netlify/functions/room"
  status = 200
  force = true

# SPA fallback. Must come after the API redirect.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/api/*"
  [headers.values]
    Cache-Control = "no-store"
